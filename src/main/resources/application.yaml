spring:
  application:
    name: bookshelf-manager
  datasource:
    url: jdbc:postgresql://localhost:5434/bookshelf
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
